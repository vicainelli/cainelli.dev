---
import type { SocialLink } from "@/data/social-links";

interface Props {
	links: SocialLink[];
}

const { links } = Astro.props;
---

<ul class="flex flex-col gap-2">
  {links.map((link) => (
    <li class="">
      <a href={link.url} class="link text-sm">{link.name}</a>
    </li>
  ))}
</ul>
