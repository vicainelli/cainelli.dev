---
interface Props {
  links: {
    title: string;
    url: string;
  }[];
}

const { links } = Astro.props;
---

<ul class="flex flex-col gap-4">
  {links.map((link) => (
    <li class="">
      <a href={link.url} class="link">{link.title}</a>
    </li>
  ))}
</ul>
